interface:
  display_name: "PRD Annotator"
  short_description: "Maintain prototype annotations and page or total PRDs"
  default_prompt: "Use $prd-annotator to read current prototype annotations, update relevant PRDs, and preserve all permanent data."
